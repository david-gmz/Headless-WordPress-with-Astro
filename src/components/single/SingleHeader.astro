---
interface Props {
  title: string;
  category: string;
  date: string;
  readingTime: string;
}

const { title, category, date, readingTime } = Astro.props;
---

<header class="relative py-16 bg-muted/50">
  <div class="container">
    <div class="max-w-3xl">
      <p class="text-sm text-muted-foreground mb-3">{category} • {date}</p>
      <h1 class="text-4xl font-bold mb-4">{title}</h1>
      <p class="text-muted-foreground">{readingTime} min de lectura</p>
    </div>
  </div>
</header>